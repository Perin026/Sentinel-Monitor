"""Small, dependency-free helpers shared across modules — kept genuinely
small on purpose (mirrors js/core/utils.js's own scope discipline). If a
helper only makes sense for one module, it belongs in that module, not here."""
