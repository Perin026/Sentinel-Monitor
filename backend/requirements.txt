# Sentinel Core Server — Phase 5.1 backend foundation dependencies.
# Pinned to a minimum version with room for patch/minor updates; re-pin
# exact versions (`pip freeze`) once this ships to production.

# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.12

# --- Configuration ---
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
pyyaml>=6.0,<7.0

# --- Database ---
sqlalchemy[asyncio]>=2.0,<3.0
aiosqlite>=0.20,<1.0
asyncpg>=0.30,<1.0
alembic>=1.13,<2.0

# --- Scheduler ---
apscheduler>=3.10,<4.0

# --- Logging ---
structlog>=24.4,<25.0

# --- Testing ---
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
httpx>=0.27,<1.0
